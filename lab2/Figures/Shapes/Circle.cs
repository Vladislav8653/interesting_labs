namespace Figures.Shapes
{
    public class Circle : Ellipse
    {
        /*public void DrawCircle(Graphics g, Point startPoint, int radius, Pen pen)
        {
            var rightBottomAngle = new Point(startPoint.X + radius, startPoint.Y - radius);
            DrawEllipse(g, startPoint, rightBottomAngle, pen);
        }*/
    }
}
