namespace Figures.Shapes
{
    public class Rhombus : Line
    {
        /* public void DrawRhombus(Graphics g, Point leftAngle, Point topAngle, Pen pen)
         {
             int stepX = Math.Abs(topAngle.X - leftAngle.X);
             int stepY = Math.Abs(topAngle.Y - leftAngle.Y);
             Point rightAngle = new Point(leftAngle.X + 2 * stepX, leftAngle.Y);
             Point bottomAngle = new Point(topAngle.X, topAngle.Y + stepY * 2);
             DrawLine(g, leftAngle, topAngle, pen);
             DrawLine(g, topAngle, rightAngle, pen);
             DrawLine(g, rightAngle, bottomAngle, pen);
             DrawLine(g, bottomAngle, leftAngle, pen);
         }*/
    }
}
